{
  "name": "test-coverage-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "pm2 start --env development",
    "build:dev": " pm2 start --env development",
    "build:test": "pm2 start --env test",
    "build:uat": "pm2 start --env pre",
    "build": "pm2 start --env production"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "make-dir": "^3.1.0",
    "morgan": "^1.10.0",
    "pm2": "^5.3.0",
    "redis": "^4.6.5"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
